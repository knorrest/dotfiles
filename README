# dotfiles

This repo is intended for use as a bare repository with the HOME directory as work tree
* git init --bare $HOME/.dotfiles
* alias dotfiles='git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME'

